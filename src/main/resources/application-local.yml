logging:
  level:
    com.volleymetrics: DEBUG
server:
  port: 6301
urls:
  localhost: http://localhost
  self: ${urls.localhost}:6301
  service:
#    helper: ${urls.localhost}:6302
    job: ${urls.localhost}:6301
#    message: ${urls.localhost}:6303